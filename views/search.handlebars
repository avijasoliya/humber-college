<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Search</title>
    <link rel="stylesheet" href="../public/styles.css">
</head>
<body>
    <header>
        <h1>Restaurant Search</h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <div class="container">
            <form action="/search" method="POST">
                <label for="page">Page:</label>
                <input type="number" name="page" id="page" min="1" value="1">
                <label for="perPage">Per Page:</label>
                <input type="number" name="perPage" id="perPage" min="1" max="50" value="10">
                <label for="borough">Borough:</label>
                <input type="text" name="borough" id="borough">
                <button type="submit">Search</button>
            </form>

            {{#if errors}}
            <div class="alert">
                {{#each errors}}
                <p>{{this.msg}}</p>
                {{/each}}
            </div>
            {{/if}}

            {{#if results}}
            <div class="results">
                <h2>Search Results</h2>
                <ul>
                    {{#each results}}
                    <li>
                        <strong>Name:</strong> {{this.name}}<br>
                        <strong>Cuisine:</strong> {{this.cuisine}}<br>
                        <strong>Borough:</strong> {{this.borough}}
                    </li>
                    {{/each}}
                </ul>
            </div>
            {{else}}
            <p>No results found.</p>
            {{/if}}
        </div>
    </main>
</body>
</html>
